(() => {
  "use strict";
  const form=document.getElementById("converterForm"),input=document.getElementById("sourceInput"),counter=document.getElementById("characterCounter"),output=document.getElementById("glyphOutput"),normalizedText=document.getElementById("normalizedText"),technicalReading=document.getElementById("technicalReading"),statusBadge=document.getElementById("statusBadge"),noticeList=document.getElementById("noticeList"),speakButton=document.getElementById("speakButton"),approximateButton=document.getElementById("approximateButton"),copyButton=document.getElementById("copyButton"),shareButton=document.getElementById("shareButton"),actionStatus=document.getElementById("actionStatus"),historySection=document.getElementById("historySection"),historyList=document.getElementById("historyList"),clearHistoryButton=document.getElementById("clearHistoryButton");
  const HISTORY_KEY="iberolab:mvp:recent:v1",HISTORY_LIMIT=5;let currentResult=null;
  function updateCounter(){counter.textContent=`${Array.from(input.value).length}/${window.IberoMvp.MAX_CHARACTERS} caracteres · máximo ${window.IberoMvp.MAX_WORDS} palabras`;}
  function readHistory(){try{const parsed=JSON.parse(localStorage.getItem(HISTORY_KEY)||"[]");return Array.isArray(parsed)?parsed.filter(item=>typeof item==="string"&&item.trim()).slice(0,HISTORY_LIMIT):[];}catch{return[];}}
  function writeHistory(entries){try{if(entries.length)localStorage.setItem(HISTORY_KEY,JSON.stringify(entries));else localStorage.removeItem(HISTORY_KEY);return true;}catch{return false;}}
  function renderHistory(){const entries=readHistory();historyList.replaceChildren();historySection.hidden=entries.length===0;for(const value of entries){const button=document.createElement("button");button.className="history-item";button.type="button";button.dataset.historyValue=value;button.textContent=value;button.addEventListener("click",()=>{input.value=value;updateCounter();convert();input.focus();});historyList.append(button);}}
  function rememberResult(result){if(result.executionStatus!=="success")return;const value=result.original.trim();if(!value)return;const key=value.toLocaleLowerCase("es-ES");const entries=[value,...readHistory().filter(item=>item.toLocaleLowerCase("es-ES")!==key)].slice(0,HISTORY_LIMIT);if(writeHistory(entries))renderHistory();}
  function createSignCard(token){const card=document.createElement("div");card.className="sign-card";card.setAttribute("role","img");card.setAttribute("aria-label",`Signo ibérico normalizado de referencia para ${token}`);const image=document.createElement("img");image.src=window.IberoMvp.SIGN_PATHS[token];image.alt=`Signo ibérico normalizado para ${token}`;image.loading="eager";const fallback=document.createElement("div");fallback.className="glyph-fallback";fallback.textContent=`${token}\nimagen no cargada`;image.addEventListener("error",()=>card.classList.add("failed"),{once:true});const label=document.createElement("div");label.className="sign-token";label.textContent=token;card.append(image,fallback,label);return card;}
  function renderNotices(result){noticeList.replaceChildren();const combined=[...result.operations.map(item=>({...item,severity:"information"})),...result.notices];if(!combined.length){const item=document.createElement("div");item.className="empty-explanation";item.textContent="La entrada se ha representado con correspondencias directas dentro de las convenciones del MVP.";noticeList.append(item);return;}for(const notice of combined){const item=document.createElement("div");item.className=`notice-item ${notice.severity}`;item.textContent=notice.message;noticeList.append(item);}}
  function renderResult(result){currentResult=result;output.replaceChildren();document.documentElement.dataset.mvpResult=result.executionStatus;document.documentElement.dataset.mvpStatus=result.status;if(result.executionStatus==="blocked"){const message=document.createElement("div");message.className="blocked-message";message.textContent=result.notices[0].message;output.append(message);normalizedText.textContent="Entrada bloqueada";technicalReading.textContent="—";statusBadge.className="badge blocked";statusBadge.textContent="No se ha generado una salida";speakButton.disabled=true;approximateButton.disabled=true;copyButton.disabled=true;shareButton.disabled=true;actionStatus.textContent="";renderNotices(result);return;}result.words.forEach((word,index)=>{if(index>0){const separator=document.createElement("span");separator.className="word-separator";separator.setAttribute("aria-hidden","true");separator.textContent="·";output.append(separator);}const group=document.createElement("div");group.className="word-group";group.dataset.sourceWord=word.source;const wordLabel=document.createElement("div");wordLabel.className="word-label";wordLabel.textContent=word.source;const signs=document.createElement("div");signs.className="word-signs";signs.append(...word.tokens.map(createSignCard));group.append(wordLabel,signs);output.append(group);});normalizedText.textContent=result.normalized;technicalReading.textContent=result.technicalReading;statusBadge.className=`badge ${result.status}`;statusBadge.textContent=result.status==="direct"?"Adaptación directa":"Adaptación con aproximaciones";const speechAvailable="speechSynthesis" in window;speakButton.disabled=!speechAvailable;approximateButton.disabled=!speechAvailable;copyButton.disabled=false;shareButton.disabled=false;actionStatus.textContent="";renderNotices(result);}
  function resultUrl(value=input.value){const url=new URL(location.href),trimmed=value.trim();if(trimmed)url.searchParams.set("q",trimmed);else url.searchParams.delete("q");return url;}
  function convert({updateUrl=true,remember=true}={}){const result=window.IberoMvp.convert(input.value);renderResult(result);if(remember)rememberResult(result);if(updateUrl){const url=resultUrl();history.replaceState(null,"",`${url.pathname}${url.search}${url.hash}`);}}
  function loadFromUrl(){const sharedInput=new URL(location.href).searchParams.get("q");if(sharedInput!==null)input.value=sharedInput;updateCounter();convert({updateUrl:false,remember:false});}
  function experimentalReadingText(result){const modernLabels={"ŕ":"r","ś":"s","ń":"n"};return result.words.map(word=>word.tokens.map(token=>modernLabels[token]||token).join("")).join(" ");}
  form.addEventListener("submit",event=>{event.preventDefault();convert();});input.addEventListener("input",updateCounter);for(const button of document.querySelectorAll("[data-example]")){button.addEventListener("click",()=>{input.value=button.dataset.example;updateCounter();convert();input.focus();});}
  clearHistoryButton.addEventListener("click",()=>{if(writeHistory([])){renderHistory();actionStatus.textContent="Historial local borrado.";setTimeout(()=>{actionStatus.textContent="";},1600);}else actionStatus.textContent="El navegador no ha permitido borrar el historial local.";});
  speakButton.addEventListener("click",()=>{if(!currentResult||currentResult.executionStatus!=="success"||!("speechSynthesis" in window))return;speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(currentResult.original);utterance.lang="es-ES";utterance.rate=.9;speechSynthesis.speak(utterance);});
  approximateButton.addEventListener("click",()=>{if(!currentResult||currentResult.executionStatus!=="success"||!("speechSynthesis" in window))return;speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(experimentalReadingText(currentResult));utterance.lang="es-ES";utterance.rate=.78;speechSynthesis.speak(utterance);actionStatus.textContent="Reproduciendo una lectura moderna aproximada de los signos.";setTimeout(()=>{actionStatus.textContent="";},2200);});
  copyButton.addEventListener("click",async()=>{if(!currentResult||currentResult.executionStatus!=="success")return;try{await navigator.clipboard.writeText(currentResult.technicalReading);copyButton.textContent="Lectura copiada";actionStatus.textContent="La lectura técnica se ha copiado.";setTimeout(()=>{copyButton.textContent="Copiar lectura técnica";actionStatus.textContent="";},1600);}catch{copyButton.textContent="No se pudo copiar";actionStatus.textContent="El navegador no ha permitido copiar la lectura.";}});
  shareButton.addEventListener("click",async()=>{if(!currentResult||currentResult.executionStatus!=="success")return;const url=resultUrl(currentResult.original).toString(),text=`IberoLab — ${currentResult.original}\n${currentResult.technicalReading}\nAdaptación fonética experimental; no es una traducción.`;try{if(typeof navigator.share==="function"){await navigator.share({title:"IberoLab",text,url});actionStatus.textContent="Resultado compartido.";}else{await navigator.clipboard.writeText(`${text}\n${url}`);actionStatus.textContent="Enlace y lectura copiados para compartir.";}setTimeout(()=>{actionStatus.textContent="";},2200);}catch(error){if(error?.name!=="AbortError")actionStatus.textContent="No se pudo compartir el resultado.";}});
  addEventListener("popstate",loadFromUrl);renderHistory();loadFromUrl();document.documentElement.dataset.mvpConverterReady="true";
})();
